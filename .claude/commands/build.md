---
description: Build the AV stack on the remote server.
---

Invoke the `build-remote` skill to compile on the remote build/run host. Assume the source was already synced; if not, run `/sync` first.
