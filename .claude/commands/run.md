---
description: Run the AV stack (mapping / bag replay / live) on the remote server.
---

Invoke the `run-remote` skill to launch the AV pipeline on the remote build/run host. Assume the latest build is present; if not, run `/build` first.
